name: MacRDP
on: 
  workflow_dispatch:
jobs:
  build:
    name: MacRDP
    runs-on: macos-latest
    
    steps:                 
    - name: Enable VNC
      run: |
        sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart -activate -configure -access -on -clientopts -setvnclegacy -vnclegacy yes -setvncpw -vncpw "}~k@)4HgYV[l\W1" -restart -agent -privs -all

    - name: Connection Info
      run: |
        echo "===================================================="
        echo "  Username: runneradmin"
        echo "  Password: }~k@)4HgYV[l\W1"
        echo "  VNC Port: 5900"
        echo "===================================================="

    - name: MacOS System running...
      uses: mxschmitt/action-tmate@v2
